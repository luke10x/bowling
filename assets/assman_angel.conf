# assman_angel.conf — animation export config for Angel
# Lines are: `mesh <MeshName>` and `clip <AnimationName>`.
# Names must match the GLB (Blender action / animation names).

mesh AngelMesh
clip BowlingThrow
clip BowlingArgument
clip lowRun
clip runHandsFront
clip throw
